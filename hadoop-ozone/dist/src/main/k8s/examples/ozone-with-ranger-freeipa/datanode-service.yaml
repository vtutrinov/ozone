apiVersion: v1
kind: Service
metadata:
  name: datanode
spec:
  publishNotReadyAddresses: true
  clusterIP: None
  selector:
    app: ozone
    component: datanode
  ports:
  - { name: rpc, port: 9864 }
